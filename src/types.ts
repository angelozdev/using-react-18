export interface User {
  last_name: string;
  first_name: string;
  avatar: string;
  email: string;
  id: number;
}

export interface Color {
  id: number;
  name: string;
  year: number;
  color: string;
  pantone_value: string;
}
